#version 330 core

smooth in vec3 fragColor;
// Ouput data
out vec3 color;

struct SunLight
{
    vec3 directon;
    vec3 color;
    float ambient;
};

uniform SunLight sunlight;

void main()
{
    color = fragColor;
}
